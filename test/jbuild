(rule ((targets (conv7))
       (deps (conv7.c utf7.c utf8.c utf7.h utf8.h getopt.h))
       (action (run gcc ${^} -o ${@}))))

(executable
 ((name test)
  (libraries (alcotest uutf fmt yuscii))))

(alias
 ((name runtest)
  (deps (test.exe conv7))
  (action (run ${<}))))
